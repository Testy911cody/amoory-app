/** Placeholder — copy `.env.example` to `.env.local` and run `npm run build` or `node scripts/inject-config.js`. Do not commit live keys. */
export const config = {
  supabaseUrl: "YOUR_SUPABASE_URL",
  supabaseAnonKey: "your_anon_key_here",
};
